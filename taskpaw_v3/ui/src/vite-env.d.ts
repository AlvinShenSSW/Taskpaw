/// <reference types="vite/client" />

// Injected by Vite `define` from package.json (see vite.config.ts) — the single
// source of truth for the displayed app version (#170).
declare const __APP_VERSION__: string;
