{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Locked down (design §3.1): the webview gets NO Tauri IPC/FS/shell permissions. The UI talks only to the local backend over HTTP on the loopback origin.",
  "windows": ["main"],
  "permissions": []
}
