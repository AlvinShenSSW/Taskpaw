# TaskPaw V2 - Windows Server Agent
# tkinter is built into Python, no installation needed

# Optional: system tray support
pystray>=0.19.0,<1.0.0
Pillow>=10.0.0,<11.0.0

# Required: fast cross-platform CPU/RAM stats (replaces wmic on Windows,
# which is deprecated on Win11 and was the dominant cause of UI freezes)
psutil>=5.9.0,<6.0.0

# Optional: package as .exe
# pyinstaller>=6.0.0

# Note: paramiko is no longer needed in V2 (SSH removed)
# Note: TaskPaw Hub (Mac Mini) has no extra dependencies
